// Generated by CoffeeScript 1.4.0
var table;

table = null;

describe("Table: ", function() {
  it('window.Shotgun is object', function(done) {
    assert(window.Shotgun);
    assert(typeof window.Shotgun === 'object');
    return done();
  });
  it('window.Shotgun.Loader is function', function(done) {
    assert(window.Shotgun);
    assert(typeof window.Shotgun.Loader === 'function');
    return done();
  });
  return it('constructor', function(done) {
    var loader;
    loader = new window.Shotgun.Loader;
    assert(loader);
    return done();
  });
});
