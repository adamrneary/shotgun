{
  "scripts": {
    "postinstall": "bower install --quiet",
    "test": "./script/start-fake-server && testem -f test/testem.json ci && ./script/sto-fake-server",
    "start": "./script/start-fake-server && testem -f test/testem.json && ./script/sto-fake-server",
    "stat": "cloc index.js --quiet && cloc test/*.js --quiet"
  },
  "devDependencies": {
    "bower": "~1.0.x",
    "testem": "~0.3.x",
    "express": "~3.3.x"
  }
}
